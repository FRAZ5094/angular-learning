# Solution: 00 — Setup

Key files after completing this section:

- `.postcssrc.json` — PostCSS configuration for Tailwind v4
- `src/styles.css` — Global styles with Tailwind import
- `src/app/app.html` — Root template with styled heading
