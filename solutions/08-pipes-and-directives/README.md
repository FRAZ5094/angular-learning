# Solution: 08 — Pipes & Directives

Key files:

- `relative-time.pipe.ts` — Custom pipe for relative time display
- `priority-highlight.directive.ts` — Custom directive for priority-based border color
