# Solution: 12 — Testing & Next Steps

Key files:

- `task.service.spec.ts` — Tests for TaskService CRUD and computed signals
- `task-card.spec.ts` — Tests for TaskCard component rendering and outputs
- `auth.guard.spec.ts` — Tests for the auth guard
