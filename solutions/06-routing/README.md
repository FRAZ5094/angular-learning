# Solution: 06 — Routing

Key files:

- `app.routes.ts` — Route configuration with lazy loading
- `app.config.ts` — Updated with `withComponentInputBinding()`
- `app.html` — Navbar + router-outlet
- `navbar.ts` — Navigation bar with active link highlighting
- `dashboard.ts` — Dashboard page
- `task-detail.ts` — Task detail page with route param
