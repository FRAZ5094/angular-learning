# Solution: 10 — Authentication

Key files:

- `auth.service.ts` — Authentication service with login/register/logout
- `auth.guard.ts` — Route guard for protected routes
- `auth.interceptor.ts` — HTTP interceptor for JWT tokens
- `login.ts` — Login page with form
- `register.ts` — Registration page with form
