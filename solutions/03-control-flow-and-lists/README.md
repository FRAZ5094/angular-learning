# Solution: 03 — Control Flow & Lists

Key files:

- `task.model.ts` — Task interface
- `task-list.ts` — TaskList component with sample data
- `task-list.html` — Template with @for, @if, @switch, @empty
